# メール送信(カスタムSMTP)の設定手順

作成日: 2026-07-26 / 対象: ルーティンノート β1（アカウント機能のメール送信）

**この設定はあなたの手作業が必要**（外部サービスのアカウントが要るため）。
設定が終われば、登録メールとパスワード再設定メールが安定して届くようになる。

---

## 0. なぜ必要か

Supabaseの**組み込みメール送信は「1時間あたり2通」**が上限。
これは無料プランの制約ではなく組み込みメーラー自体の仕様で、
**ダッシュボードからは変更できない**（公式に「カスタムSMTPでしか変えられない」と明記されている）。

テスターが3〜5人いると、同じ時間帯に登録した時点で3人目から確実に弾かれる。
「試行が多すぎます／email rate limit exceeded」はこれが原因。

> ✅ カスタムSMTPを設定すると、**Authentication → Rate Limits** で送信数を自分で決められるようになる
> （既定は1時間あたり30通）。

---

## 1. どのサービスを使うか

| | 独自ドメイン | 無料枠 | 備考 |
|---|---|---|---|
| **Gmail (SMTP)** | **不要** | 1日約500通 | **ドメインが無いならこれが最短。** β規模には十分 |
| Brevo | 不要（送信元アドレス単位の認証） | 1日300通 | Gmail以外で送りたい場合 |
| Resend | **必須** | 月3,000通 | 独自ドメインを取ったら乗り換え先として最有力 |

**独自ドメインを持っていないなら Gmail で始めるのが速い。**
将来 `routine-note.jp` のようなドメインを取ったら Resend へ移行すると、到達率と体裁が上がる。

---

## 2. Gmailで設定する（ドメイン不要）

### 2-1. アプリパスワードを作る

Googleは通常のパスワードでのSMTP接続を許可していないので、専用の「アプリパスワード」を発行する。

1. Googleアカウントで**2段階認証をONにする**（アプリパスワードの発行にはこれが必須）
   → https://myaccount.google.com/security
2. https://myaccount.google.com/apppasswords を開く
3. 名前を `Supabase routine-note` などにして作成
4. 表示される**16桁のパスワードをコピー**（この画面を閉じると二度と見られない）

> ⚠️ アプリパスワードは、Googleアカウントのパスワードを変更すると自動的に無効になる。
> その場合はもう一度発行して、Supabase側も更新する。

### 2-2. Supabaseに登録する

Supabaseダッシュボード → **Authentication → Emails → SMTP Settings**（画面名は版により多少異なる）
で **Enable Custom SMTP** をONにして、次を入力する。

| 項目 | 値 |
|---|---|
| Host | `smtp.gmail.com` |
| Port | `465`（SSL） ※うまくいかなければ `587` |
| Username | あなたのGmailアドレス（例 `circusarata@gmail.com`） |
| Password | 2-1で作った**16桁のアプリパスワード**（Googleアカウントのパスワードではない） |
| Sender email | **Usernameと同じアドレス**（Gmailは差出人の詐称を許さないため、ここを別のアドレスにすると送信できない） |
| Sender name | `ルーティンノート` など |

保存したら、**Authentication → Rate Limits** で「Emails per hour」を必要な値（例: 30）に設定する。

### 2-3. 動作を確認する

アプリの **設定 → アカウント → ログイン / アカウントを作る → アカウントを作る** から、
自分のメールアドレスで登録してみる。

- 確認メールが届く（**迷惑メールフォルダも見る**。初回は入りやすい）
- リンクを開くとアプリに戻り、ログイン済みになる
- 「パスワードを忘れた」からも再設定メールが届く

---

## 3. うまくいかないときの切り分け

| 症状 | 原因と対処 |
|---|---|
| 「試行が多すぎます」が続く | まだ組み込みメーラーのまま。Custom SMTPが有効になっているか確認。有効化直後は1時間の枠がリセットされるまで残っている場合がある |
| メールが全く届かない | Usernameとアプリパスワードの誤り。Sender emailがUsernameと違うとGmailは拒否する |
| 迷惑メールに入る | Gmail送信では起こりやすい。テスターに「迷惑メールを確認して」と伝える。独自ドメイン＋Resendに移行すると改善する |
| リンクを開いてもアプリに戻らない | **Authentication → URL Configuration** の Site URL / Redirect URLs に<br>`https://routine-note.pygmix.com/` が入っているか確認 |

---

## 4. 独自ドメインを取った場合（将来）

Resendへ移行すると到達率が上がり、差出人も `noreply@あなたのドメイン` にできる。

1. https://resend.com/ に登録し、ドメインを追加してDNS（DKIM/SPF）を設定・検証
2. APIキーを作成
3. SupabaseのSMTP設定を次に変更
   - Host `smtp.resend.com` / Port `465`（または`587`）
   - Username `resend`（固定）
   - Password **APIキー**
   - Sender email `noreply@あなたのドメイン`

---

## 関連

- `SUPABASE_SETUP.md` … プロジェクト作成・スキーマ・キーの取得
- `HANDOFF_CODEX.md` … プロジェクト全体の現在地
