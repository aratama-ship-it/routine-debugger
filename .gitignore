PROJECT_NOTES.md
HANDOFF_CODEX.md
.DS_Store

# iCloudの同期競合コピー(「app 2.js」等)は追跡しない
* [0-9].*

# アイコン制作の作業物(検討案・旧アイコンの退避)。配信には不要なのでローカルのみ
icons/candidates/
icons/_old/

# 利用状況の見かた。テスターのメールアドレスを引く手順を含むので、
# docs/ の公開範囲を決めるまではローカルのみに置く
ANALYTICS_SETUP.md

# β前レビューの依頼書(内部監査結果を含むためローカルのみ)
CODEX_REVIEW_BETA.md

# 設計メモ・調査・監査の記録。GitHub Pagesは全ファイルを配信するため、
# ここに置いたものは誰でも読める。収益化の検討や競合調査を含むのでローカルのみ。
docs/
